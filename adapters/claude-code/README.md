# Claude Code Adapter

This adapter uses the canonical skill folders directly.

Install by copying `../../skills/*` into your Claude skills directory and keep the root `AGENTS.md` instructions available in your repo or workspace.

